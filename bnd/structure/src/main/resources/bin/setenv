#!/bin/sh
#
# handle specific scripts; the SCRIPT_NAME is exactly the name of the GATEWAY
# script: gateway
#
# if [ "${GATEWAY_SCRIPT}" == "SCRIPT_NAME" ]; then
#   Actions go here...
# fi

#
# general settings which should be applied for all scripts go here; please keep
# in mind that it is possible that scripts might be executed more than once, e.g.
# in example of the start script where the start script is executed first and the
# GATEWAY script afterwards.
#

#
# The following section shows the possible configuration options for the default 
# GATEWAY scripts
#
# export JAVA_HOME # Location of Java installation
# export JAVA_MIN_MEM # Minimum memory for the JVM
# export JAVA_MAX_MEM # Maximum memory for the JVM
# export JAVA_PERM_MEM # Minimum perm memory for the JVM
# export JAVA_MAX_PERM_MEM # Maximum perm memory for the JVM
# export EXTRA_JAVA_OPTS # Additional JVM options
# export GATEWAY_HOME # GATEWAY home folder
# export GATEWAY_DATA # GATEWAY data folder
# export GATEWAY_BASE # GATEWAY base folder
# export GATEWAY_ETC  # GATEWAY etc  folder
# export GATEWAY_SYSTEM_OPTS # First citizen GATEWAY options
# export GATEWAY_OPTS # Additional available GATEWAY options
# export GATEWAY_DEBUG # Enable debug mode
# export GATEWAY_REDIRECT # Enable/set the std/err redirection when using bin/start
# export GATEWAY_NOROOT # Prevent execution as root if set to true
